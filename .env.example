# URL of the audio-processing backend (see backend/) that powers voice note
# transcription — required for the "語音筆記" (Voice Note) feature to work.
# Defaults to http://localhost:8000 for local development if unset.
VITE_BACKEND_URL=http://localhost:8000
